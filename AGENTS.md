# index-X Agent 维护规约

## 项目定位

本仓库是某系列 EPUB 档案及其构建、校对工具仓库。`EPUB/` 是需要版本化保存的档案主体；维护工具用于可重复地同步、检查和打包档案。

## 目录约定

- `EPUB/`：版本化的 EPUB 解压目录，属于发布源文件。
- `docs/`：项目说明和需要长期保留的维护记录。
- `tools/`：可重复使用、经过验证的维护工具。
- `.github/workflows/`：发布和自动化流程。
- `.cache/`：本地缓存、解压副本、规范化文本、审计 JSON/报告；不得提交。

## 维护流程

1. 同步 OneDrive 内容前先确认来源目录可读；使用 `tools/sync-epub-from-onedrive.ps1 -SourceDirectory <路径> -WhatIf` 预览范围。
2. 同步后确认日文缓存已由外部流程准备，再运行 `python tools/epub_audit.py`。
3. 检查 `git status`、变更文件数、`git diff --stat`，并抽查文本 diff。
4. 需要发布时再执行 EPUB 打包工作流；不要把构建输出复制回 `EPUB/`。

## 版本控制边界

- 应提交：可重复使用的维护脚本、工具说明、项目规约，以及确认过的档案内容修改。
- 不应提交：`.cache/`、临时 `scan_*` 脚本、一次性诊断输出、机器本地路径和中间报告。
- 长期保留的检查结论应写入 `docs/maintenance-records/`，只保留结论、范围、统计和必要样例，不复制整个缓存。
- 不要为了格式化而批量改写 EPUB；保持文件名、目录结构、编码和换行行为稳定。

## Agent 操作边界

- 默认只修改工作区，不自动 commit、push、创建 release 或删除档案。
- 不回滚用户已有修改；发现同步覆盖或大批量变化时先报告统计和代表性 diff。
- 修改同步、审计或发布工具后，至少运行一次对应命令验证；失败项必须在结果中明确列出。
- 日文缓存必须是 EPUB 原样解压结果；术语定位直接使用缓存内的原始 XHTML，不得依赖规范化单行报告推断行号。
- 处理外部目录时使用显式路径，并避免把 OneDrive、临时目录或个人环境信息写入仓库文件。

## 变更说明

维护工具的行为变更应同步更新 `tools/README.md`。如果新增缓存目录、报告目录或构建产物，必须同时更新 `.gitignore` 和本文件。
