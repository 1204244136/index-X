# 中日缓存表头结构规范化（2026-08-15）

## 结论

按 AGENTS.md 表头规范（前 3 行固定为 XML 声明 / DOCTYPE / html-head-title-body 合并行，`<h1>` 独立落在第 4 行，`<h1>`+`<h2>` 依次占第 4、5 行，删除头部填充 `<br/>`）对 `.cache/epub-work/` 中日两侧做了全量整理。中文侧 762 个带表头内容文件全部达标（含 SP 篇首插图合并新规）；日文侧 873 个配对文件完成 808 个，其余为图片页（不适用）与下述冲突清单。中日表头行数对齐从整理前的 0/743 提升到 738/743，剩余 5 对差异均为内容级冲突，见清单。

## 范围与统计

- 中文侧 `.cache/epub-work/chinese-text`：762 个带表头正文文件。
  - 提取 `<h1>` 到第 4 行 676 个；仅删填充 `<br/>` 21 个；清除裸 `<div>` 包装 10 个；SP 篇插图与标题合并 4 个。
  - 所有修改经标签流与正文内容双重校验，XML 全部合法。
- 日文侧 `.cache/epub-work/japanese-text`：873 个配对文件（命名含 `_p-N`、`-p-N`、裸表头、`_N` 四种变体，覆盖 S1-S6 全部书目）。
  - 提取 `<h1>`（含 start-3em/p 包装折叠）679 个；p 型标题（序章/あとがき等）移到第 4 行 67 个；数字小节标题折叠为独立 `<h2>` 35 个；删除头部填充 `<br/>`；`<div class="main">` 并入第 3 行。
  - 42 个纯图片页按「纯图片页不适用」跳过。
- 行数对齐：738/743 表头完全一致（含 S1_25 SP 与 S5/S6）。

## 冲突清单（按用户要求停止修改并列出）

| 文件/表头 | 冲突内容 |
|---|---|
| 12 个 S1 后记附页（S1_02-11、S1_04-09 等） | 头部畸形：`<head></head><body>` 之后重复出现 `meta/title/link` 与 BOM，无法安全折叠 |
| S1_25-Stiyl_Magnus（日文） | 原 EPUB 缺少 `<body>` 标签，XML 本身不合法 |
| S3_08-02、S3_08-06 | `<h1>` 嵌在 `<p>` 文本中间（标题前后各有正文文本），无法独立成行 |
| S2_19-13 | 跨页后记特例，保留原文（含文末 6 个连续换行与原始闭合标签布局） |
| S1_02-08、S2_09-07 | 日文有数字小节标题 `<h2>１</h2>`，中文对应位置没有，且规范禁止向中文添加行 |
| S4_03-06 | 日文该章没有标题行（标题为 `<p>` 且结构异常），中文有 `<h1>` |
| 9 个 fixed-layout 页（S3_11-09/10、S3_14-09/10、S4_01-13/14、S4_02-14/15） | 多行 head、无中文对应页，属 JP-only 预览页 |
| S3_12-03、S3_13-06、S3_14-02..06 | 原文 XML 未声明前缀（unbound prefix），JP-only 书目，已还原 OneDrive 原样 |
| 7 个 S3_09/10 尾部页（-13/-14、-09/-10） | JP-only 页，结构畸形 |

## 必要样例

中文 Prologue（提取 h1 + 删 br，行数净零）：

```
3: <html …><head>…</head><body>
4: <h1><div style="text-align: center;"><span>序　章 <br/></span>…</div></h1>
5: <p>「——啧！可恶！…</p>
```

日文 Chapter1（折叠 start-3em 包装，数字标题折叠为 h2）：

```
3: <html class="vrtl" …><head>…</head><body class="p-text"><div class="main">
4: <h1>第一章　まるで遊園地のような　Red_Wear,Big_Bag,and_Flying_Sledge.</h1>
5: <p>１</p>
6: <p>夢であって欲しかった。</p>
```

SP 篇（插图与标题合并为第 4 行，Stiyl 篇调整为图片在前）：

```
4: <p><img …S1_25-Kamijou_Touma.jpg"/></p><h1 class="color1" id="toc_1">上条当麻篇</h1>
5: <p>『……沙沙……听得……见吗…</p>
```

## 备注

- 处理脚本为一次性诊断脚本，未提交仓库；规范已写入 AGENTS.md（含 SP 篇首插图特别规定）。
- `normalize_epub_cache.py` 与 `tools/README.md` 描述的旧行为（日文章节标题并入 HTML 行、小节编号第 4 行）已被新规范取代，工具尚未同步更新，后续维护时需注意。
- EPUB/ 中两处未提交修改（S1_25-Stiyl_Magnus 插图分行、S2_15-10_Chapter5 正文订正）经核对已存在于 `.cache/`（后者逐字一致），发布后 EPUB/ 将被缓存版本覆盖，属预期。
