# 「空中分解」语义分轨统一（2026-09-14）

- 范围：`EPUB/` 归档（中文 25 文件）＋ `.cache/epub-work/chinese-text/`
- 方法：按中日表头配对的物理行逐条比对；使用一次性显式映射与预检，改完即删
- 本轮结果：统一 31 处；三处语义偏离另案处理

## 背景

日文全库精确命中「空中分解」59 处。其中 48 处有中文对应，11 处所在作品暂无中文缓存。

原中文处理混用了「空中分解」「空中解体」「分解」「解体」「分崩离析」「瓦解」
「崩溃」「解散」「中道崩殂」「四分五裂」等多种写法。该词并非单一语境：

- 飞机、航天器、天体、机械和物体指实际发生的空中结构破裂；
- 肉体、术式、第三只手等指超自然构造崩坏；
- 组织、团队、结社和团体指内部关系断裂后崩溃。

日文全库没有精确使用「消散」。「消散」已有独立语义层，主要用于 `消える／消滅`，
因此不纳入本轮映射。

## 判定口径

| 日文语境 | 规范中文 | 本轮改动 | 边界 |
| --- | --- | ---: | --- |
| 空中实际发生的结构破裂 | **空中解体** | 9 | 保留“在空中”的空间信息 |
| 肉体、术式、超自然构造崩坏 | **崩解** | 12 | 不套用只适合飞行器的「空中解体」 |
| 组织、团体、结社内部崩溃 | **分崩离析** | 10 | 不按字面译成「组织在空中解体」 |

另有 11 处原译已经符合以上分轨，无需改动。两处抽象过程按上下文保留：

- `S1_03-04`：实验系统用「彻底瓦解」；
- `S2_17-06`：作战行动用「崩溃」。

以下三处不是单纯异译，而是语义偏离，留给后续独立提交处理：

- `S3_09-02`：「半途而废」丢失“已经分崩离析且无法恢复”；
- `S5_01_02-05`：「走投无路」丢失“组织随时可能分崩离析”；
- `S5_04_01-08`：完全省略“自己分崩离析”的动作。

## 结果

- 25 个 XHTML，31 insertions / 31 deletions，行数零变化。
- `check_alignment.py --strict`：检查 913 个正文文件，问题 0。
- 25 个改动 XHTML 全部通过 XML well-formed 校验。
- `git diff --check` 通过。
- 使用 `publish.py --sync-only --side chinese` 同步 `EPUB/`，未打包、未上传、未触碰 OneDrive。

本轮属一次性内容修订，不保留复用工具与映射数据；临时脚本已按
AGENTS.md「工具与记录的边界」删除。
