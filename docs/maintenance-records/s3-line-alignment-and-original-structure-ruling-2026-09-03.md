# S3 中日行数对齐与「原始结构」解释基线（2026-09-03）

## 结论

1. **规约解释裁定**：`AGENTS.md`「中日正文行结构」条目中「行数对齐时不得向任一侧添加**原始不存在的**结构标签」的「原始」，经确认指**中日任一侧的制作源**（日文 BW 分页源、中文出版源），而非「目标侧文件当前状态」。同日已将该句修订为「原始作品中不存在的结构标签……来凑行数」，并补充「结构元素只要在制作源中真实存在、仅被某侧制作管线丢失，允许在对应位置补回，属恢复源结构而非对齐填充」。判定依据：AGENTS 自身多处明文许可向缺失侧补入结构（「日文缺失的 h2 小节补在对应空行位置」「SP 篇目日文侧补 h1」「S6 中文侧插图应补入日文侧」），按「目标侧当前没有」的读法这些条款将与 113 行自相矛盾。
2. **S3 全系列中日行数对齐完成**：修订后按「恢复源结构」向中文缓存补回日文源中真实存在、被翻译管线丢失的 `<br/>`，S3 配对文件（含全部后记）行数与结构全部对齐。

## 范围与统计

- 预处理规则（`tools/bw_extract_preprocess.json` v5→v6，25→28 条）：新增「图片前填充br清理」「图片后填充br清理」（紧贴整段插图上下的填充 `<br/>`，仅匹配纯图片段落，gaiji 与正文场景 `<br/>` 不受影响）、「align-end单段容器折叠为右对齐p」（后记署名块等单段容器 3 行折叠为 1 行）。
- 分页映射（`tools/bw_page_header_overrides.json` 新增 9 本）：`S3_02`~`S3_11` 后记（あとがき）之后的次巻予告文本页与预览插图页标 `null`（包装页，不并入后记）；`S3_01`、`S3_06` 数值页范围内无后记之后页面，无需映射。
- 日文缓存重建：`S3_01`~`S3_11`、`S6_22.06.10` 共 12 本经 `bw_preprocess.py --book-id --unpacked` 重新生成；`S6_22.06.10` 缓存首次按审计映射拆分，7 章与中文侧成对。
- 中文缓存修正 27 处：闭标签分行合并 20 文件（S4_01/S4_02 等）、孤立尾随/多余 `<br/>` 删除 14 文件、段落按日文分段拆分/合并 8 文件（S3_01-01、S3_03-02、S3_03-08、S3_06-02、S3_07-01、S3_07-04、S3_10-06）、多余 br/注释行删除 4 文件（S3_03-10、S3_06-01、S3_07-08、S3_11-01）。
- **恢复源结构补回 `<br/>` 共 16 行**（依据上述裁定）：

| 文件 | 补回 | 依据（日文源双/三连 `<br/>`） |
|---|---:|---|
| S3_01-12 / S3_02-11 / S3_03-11 / S3_04-11 / S3_06-10 / S3_07-12 / S3_08-12 / S3_09-11 后记 | 各 +1 | 致谢段前双 `<br/>`（如 S3_01 p-014 raw L46-47） |
| S3_10-07 后记 | +5 | 4 组双连 + 1 组三连（JP L26-27-28） |
| S3_11-07 后记 | +2 | 2 组双连（JP L10-11、L16-17） |
| S3_11-04 正文 | +1 | 正文场景分隔单 `<br/>`（JP L188，源 p-007 L214） |

## 复查

```powershell
python tools/check_alignment.py
```

S3 系列剩余未对齐文件仅为既有较大差异（非本轮微弱偏差范围）：`S3_01-03/-05/-07/-09`（日文侧小节 h2 缺失，-8~-25）、`S3_09-01`（+4）。后记致谢前双 `<br/>` 模式同样存在于 S2 系列后记（S2_13-14、S2_15-12、S2_16-11、S2_17-11、S2_18-09、S2_21-11 等），如需对齐可按同一裁定补回。
